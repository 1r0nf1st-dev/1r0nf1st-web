# Frontend configuration
# API server base URL
# Leave empty or unset for development (uses /api with Vite proxy)
# Set to full URL for production (e.g., https://api.yoursite.com)
# VITE_API_BASE_URL=

# Server-side configuration (used by Express server in /server)
# GitHub username to fetch repositories for
GITHUB_USERNAME=your-github-username

# Optional: GitHub personal access token for higher rate limits
# Never commit real tokens. This file is for documentation only.
GITHUB_TOKEN=your-github-token

# Medium RSS feed: set MEDIUM_FEED_URL (full URL) or MEDIUM_USERNAME (e.g. 1r0nf1st)
# Example: MEDIUM_FEED_URL=https://medium.com/feed/@1r0nf1st
# Or: MEDIUM_USERNAME=1r0nf1st
# MEDIUM_FEED_URL=
# MEDIUM_USERNAME=1r0nf1st

# Spotify (for "Recently played" on the site)
# Create an app at https://developer.spotify.com/dashboard. Add redirect URI in the app
# (e.g. http://localhost:3001/api/spotify/callback). Visit /api/spotify/auth to authorize
# and get a refresh token; add it below.
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# SPOTIFY_REFRESH_TOKEN=
# SPOTIFY_REDIRECT_URI=http://localhost:3001/api/spotify/callback

# Strava (for activity totals on the site)
# Create an app at https://www.strava.com/settings/api. Add redirect URI in the app
# (e.g. http://localhost:3001/api/strava/callback). Visit /api/strava/auth to authorize
# and get a refresh token; add it below.
# STRAVA_CLIENT_ID=
# STRAVA_CLIENT_SECRET=
# STRAVA_REFRESH_TOKEN=
# STRAVA_REDIRECT_URI=http://localhost:3001/api/strava/callback

# Server port (defaults to 3001)
PORT=3001

# Environment (development or production)
# In production, set this to 'production'
NODE_ENV=development

# Production: Comma-separated list of allowed CORS origins
# Example: https://yoursite.com,https://www.yoursite.com
# Leave unset for development (allows all origins)
# ALLOWED_ORIGINS=

# Base URL for any other public API you want to use, if applicable.
PUBLIC_API_BASE=https://api.example.com

# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_ANON_KEY=your-anon-key (public key, safe for client-side)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (private key, server-side only)
# Note: The SERVICE_ROLE_KEY bypasses Row Level Security - keep it secret!
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
