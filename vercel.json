{
  "buildCommand": "pnpm build:server && pnpm build:client",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/[[...slug]].ts": {
      "includeFiles": "server/dist/**"
    }
  }
}
